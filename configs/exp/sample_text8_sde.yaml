# @package _global_

name: sample/text8_sde
model_path:

sampling:
  batch_per_gpu: 0
  predictor    : grw        # original SDE sampler (Euler-Maruyama)
  steps        : 256

eval:
  batch_size    : 0
  n_iters       : 200
  entropy       : True
  nll           : True
  gen_ppl       : True
  eval_model    : gptj
  simul_steps   : 500
